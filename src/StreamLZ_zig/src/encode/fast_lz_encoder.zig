//! [phase 9] Fast codec compressor (greedy / lazy parser + token encoder).
//!
//! Port of src/StreamLZ/Compression/Fast/{Compressor,FastParser,Encoder,Matcher,CostModel}.cs.
//! Produces the same wire format that `decode/fast_lz_decoder.zig` consumes.
